import Products from '../products.json';

/**
 * Fetches products from store and returns.
 * 
 * In full implementation, async database fetch would go here.
 */
const getProducts = () => {
	return Products;
}
export default getProducts;